# Playwright
node_modules/
test-results/
/playwright-report/
/reports

# The GRR whose pages this suite drives. Generated by
# generate_fixtures.py, never committed: a committed page is a snapshot
# of a template that has since moved on.
/fixtures/
