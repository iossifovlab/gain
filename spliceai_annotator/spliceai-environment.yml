# This file may be used to create an environment using:
# $ conda env create --name <env> --file <this file>
# platform: linux-64
# conda create -c conda-forge -c bioconda -n <env name> --file <this file>
# - conda-forge 

name: gain
channels:
  - conda-forge
  - bioconda
  - iossifovlab
dependencies:
  - tensorflow=2.18
  # The alternative model runtime, selected with SPLICEAI_BACKEND=onnx.
  # >=1.26 is what conda-forge actually ships (see conda-recipe/recipe.yaml).
  - onnxruntime>=1.26
  # - pip:
  #   - types-tensorflow
  