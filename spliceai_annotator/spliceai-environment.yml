# Generated by scripts/conda_env.py from the workspace
# pyprojects -- do not edit by hand. Regenerate:
#     python scripts/conda_env.py
# Install on top of environment.yml and dev-environment.yml:
#     mamba env update --name gain --file spliceai_annotator/spliceai-environment.yml
name: gain
channels:
  - conda-forge
  - bioconda
dependencies:
  - python>=3.12
  # gain-spliceai-annotator (spliceai_annotator/pyproject.toml [project.dependencies])
  - numpy>=2.2
  - onnxruntime>=1.26
  - tensorflow>=2.18
  # gain-spliceai-annotator (spliceai_annotator/pyproject.toml [dependency-groups.dev])
  - mypy==1.15.0
  - onnx==1.22.0
  - pylint
  - pytest
  - pytest-cov
  - pytest-mock
  - pytest-xdist
  - ruff==0.16.5
  - tf2onnx==1.17.0
  # pip-only: PIP_ONLY in scripts/conda_env.py
  - pip
  - pip:
    # pylint-junit: not packaged on any conda channel
    - pylint-junit
